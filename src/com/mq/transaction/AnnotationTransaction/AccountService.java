package com.mq.transaction.AnnotationTransaction;

public interface AccountService {
    void transfer01(String outer, String inner, int money);
    void transfer02(String outer, String inner, int money);
    void transfer03(String outer, String inner, int money);
    void transfer04(String outer, String inner, int money);
}
